apiVersion: v1
kind: Namespace
metadata:
  name: banking-dev
---
apiVersion: v1
kind: Service
metadata:
  name: core-banking-service-svc
  namespace: banking-dev
  annotations:
    harness.io/primary-service: "true"
  labels:
    app: core-banking-service
spec:
  type: ClusterIP
  selector:
    app: core-banking-service
  ports:
    - name: http
      port: 80
      targetPort: 8092
---
apiVersion: v1
kind: Service
metadata:
  name: core-banking-service-svc-stage
  namespace: banking-dev
  annotations:
    harness.io/stage-service: "true"
  labels:
    app: core-banking-service
spec:
  type: ClusterIP
  selector:
    app: core-banking-service
  ports:
    - name: http
      port: 80
      targetPort: 8092
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-banking-service
  namespace: banking-dev
  labels:
    app: core-banking-service
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: core-banking-service
  template:
    metadata:
      labels:
        app: core-banking-service
    spec:
      containers:
        - name: core-banking-service
          image: yashwanth536/core-banking-service:5
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8092
          env:
            - name: SERVER_PORT
              value: "8092"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql-javatodev-app:3306/banking_core_service"
            - name: SPRING_DATASOURCE_USERNAME
              value: "javatodev_development"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "oPItyPticIAt"
            - name: SPRING_CLOUD_CONFIG_ENABLED
              value: "false"

          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8092
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 18

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8092
            initialDelaySeconds: 90
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 12

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8092
            initialDelaySeconds: 120
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 5

          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "768Mi"
